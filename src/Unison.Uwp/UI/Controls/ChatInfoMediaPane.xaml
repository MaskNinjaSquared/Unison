<UserControl
    x:Class="Unison.Uwp.UI.Controls.ChatInfoMediaPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Unison.Uwp.UI.Controls"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:helpers="using:Unison.Uwp.UI.Helpers"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch">

    <UserControl.Resources>
        <Style x:Key="ChatDetailInfoEmptyIconStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource IconFont}"/>
            <Setter Property="FontSize" Value="96"/>
            <Setter Property="Foreground" Value="#66FFFFFF"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
        </Style>
        <Style x:Key="ChatDetailInfoEmptyTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="#E8FFFFFF"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="24"/>
            <Setter Property="MaxWidth" Value="280"/>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="MediaHost" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel x:Name="MediaEmptyHost"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock Text="&#xE91B;"
                       FontFamily="{StaticResource IconFont}"
                       Style="{StaticResource ChatDetailInfoEmptyIconStyle}"/>
                    <TextBlock x:Name="MediaEmptyText"
                       x:Uid="ChatDetailInfo_MediaEmpty"
                       Text="Photos, videos and audio will appear here."
                       Style="{StaticResource ChatDetailInfoEmptyTextStyle}"
                       Margin="24,16,24,24"/>
        </StackPanel>
        <ProgressRing x:Name="MediaLoadingRing"
                      Width="48"
                      Height="48"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Foreground="White"
                      IsActive="False"
                      Visibility="Collapsed"
                      IsHitTestVisible="False"/>
        <toolkit:AdaptiveGridView
            x:Name="MediaGrid"
            DesiredWidth="104"
            ItemHeight="104"
            OneRowModeEnabled="False"
            StretchContentForSingleRow="False"
            SelectionMode="None"
            IsItemClickEnabled="True"
            ItemClick="MediaGrid_ItemClick"
            SizeChanged="MediaGrid_SizeChanged"
            Loaded="MediaGrid_Loaded"
            Padding="6,4,6,8"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">
            <toolkit:AdaptiveGridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="helpers:DesktopCursor.CursorKind" Value="Hand"/>
                    <Setter Property="Margin" Value="1"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="MinHeight" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="#FF1A1A1A"/>
                    <Setter Property="UseSystemFocusVisuals" Value="False"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="GridViewItem">
                                <Grid Background="{TemplateBinding Background}"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch">
                                    <ContentPresenter Content="{TemplateBinding Content}"
                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                      HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Stretch"
                                                      HorizontalContentAlignment="Stretch"
                                                      VerticalContentAlignment="Stretch"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </toolkit:AdaptiveGridView.ItemContainerStyle>
            <toolkit:AdaptiveGridView.ItemTemplate>
                <DataTemplate>
                    <!-- Do not set CornerRadius on ChatInfoMediaTile: Control.CornerRadius is
                         1809-only and XAML-parses the whole Media pivot crash on Creators Update. -->
                    <controls:ChatInfoMediaTile HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                </DataTemplate>
            </toolkit:AdaptiveGridView.ItemTemplate>
        </toolkit:AdaptiveGridView>
    </Grid>
</UserControl>
