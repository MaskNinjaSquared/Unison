<UserControl
    x:Class="Unison.Uwp.UI.Controls.SettingBox"
    x:Name="SettingBoxControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="80"
    d:DesignWidth="400"
    MaxWidth="800"
    HorizontalAlignment="Stretch">

    <!-- Fill #1E1E1E, 1px translucent border, radius 5. -->
    <Border Background="#1E1E1E"
            BorderBrush="#40FFFFFF"
            BorderThickness="1"
            CornerRadius="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Margin="12,12,8,0"
                       FontSize="14"
                       FontWeight="SemiBold"
                       MaxLines="2"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource WhatsAppTextHighBrush}"
                       Text="{Binding Header, ElementName=SettingBoxControl}"/>

            <TextBlock Margin="12,4,8,12"
                       Grid.Row="1"
                       FontSize="12"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource WhatsAppTextMediumBrush}"
                       Opacity="0.9"
                       Text="{Binding Text, ElementName=SettingBoxControl}"/>

            <ContentPresenter Grid.Row="0"
                              Grid.RowSpan="2"
                              Grid.Column="1"
                              Margin="10,0,12,0"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              Content="{x:Bind Control, Mode=OneWay}"/>
        </Grid>
    </Border>
</UserControl>
