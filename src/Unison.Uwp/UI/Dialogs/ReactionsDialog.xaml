<ContentDialog
    x:Class="Unison.Uwp.UI.Dialogs.ReactionsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Unison.Uwp.UI.Controls"
    mc:Ignorable="d"
    Title="Reactions">

    <Grid MinWidth="280">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tally chips side by side, on a fill lighter than the dialog in both themes. -->
        <ScrollViewer Grid.Row="0"
                      Margin="0,4,0,12"
                      HorizontalScrollMode="Auto"
                      HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollMode="Disabled"
                      VerticalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="ChipsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource ReactionsDialogChipBrush}"
                                BorderBrush="{ThemeResource ReactionsDialogChipBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="10,4"
                                MinHeight="28"
                                Margin="0,0,8,0">
                            <TextBlock Text="{Binding Label}"
                                       FontFamily="{StaticResource MainFont}"
                                       FontSize="14"
                                       VerticalAlignment="Center"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <ListView x:Name="AuthorsList"
                  Grid.Row="1"
                  SelectionMode="None"
                  IsItemClickEnabled="False"
                  MaxHeight="320"
                  HorizontalAlignment="Stretch">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0,6"/>
                    <Setter Property="MinHeight" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <controls:ChatAvatarControl AvatarUrl="{Binding AvatarUrl}"
                                                    Size="40"
                                                    Width="40"
                                                    Height="40"
                                                    VerticalAlignment="Center"/>

                        <StackPanel Grid.Column="1"
                                    Margin="12,0,8,0"
                                    VerticalAlignment="Center">
                            <TextBlock Text="{Binding DisplayName}"
                                       FontFamily="{StaticResource MainFont}"
                                       FontSize="15"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Phone}"
                                       Visibility="{Binding Phone, Converter={StaticResource StringToVisibilityConverter}}"
                                       Foreground="{ThemeResource ChatDetailMutedTextBrush}"
                                       FontFamily="{StaticResource MainFont}"
                                       FontSize="12"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                        <TextBlock Grid.Column="2"
                                   Text="{Binding Emoji}"
                                   FontSize="20"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
