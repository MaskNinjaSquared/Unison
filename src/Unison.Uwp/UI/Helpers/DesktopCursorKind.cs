namespace Unison.Uwp.UI.Helpers
{
    /// <summary>Mouse pointer shape for desktop (PC) hover hints.</summary>
    public enum DesktopCursorKind
    {
        Default,
        Hand,
        Blocked
    }
}
