<UserControl
    x:Class="Unison.UWPApp.UI.Controls.MessageBubbleChrome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="64"
    d:DesignWidth="240">

    <Grid x:Name="RootGrid">
        <Grid x:Name="CompositionHost"/>

        <!-- Runtime fallback when composition NineGrid/mask is unavailable -->
        <Grid x:Name="FallbackHost">
            <Border x:Name="BodyHighlightBorder"/>
            <Border x:Name="BodyFillBorder" Margin="0,1,0,0"/>

            <Grid x:Name="TailContainer" Width="10" Height="10" VerticalAlignment="Bottom" Margin="-1,0,-1,0">
                <Path x:Name="TailHighlightPath" Stretch="Fill"/>
                <Path x:Name="TailFillPath" Stretch="Fill" Margin="0,1,0,0"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
