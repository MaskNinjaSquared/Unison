<Page
    x:Class="Unison.UWPApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unison.UWPApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Unison.UWPApp.UI.Views"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Page.Resources>
    </Page.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:ConnectionId='1' x:Name="LayoutStates">
                <VisualState x:ConnectionId='2' x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Column0.Width" Value="320"/>
                        <Setter Target="Column1.Width" Value="*"/>
                        <Setter Target="ChatListPart.Visibility" Value="Visible"/>
                        <Setter Target="ChatDetailPart.Visibility" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:ConnectionId='3' x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="Column0.Width" Value="*"/>
                        <Setter Target="Column0.MaxWidth" Value="9999"/>
                        <Setter Target="Column1.Width" Value="0"/>
                        <Setter Target="ChatDetailPart.Visibility" Value="Collapsed"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Overlay (Visible when not logged in or during pairing) -->
        <Grid x:ConnectionId='4' x:Name="MainOverlay" Visibility="Visible">
            <views:LoginView x:ConnectionId='13' x:Name="LoginPart"/>
            
            <!-- Shims for legacy code references if any remain during transition -->
            <StackPanel Visibility="Collapsed">
                <Grid x:ConnectionId='14' x:Name="LoginPanel"/><Grid x:ConnectionId='15' x:Name="QRPairingPanel"/><Grid x:ConnectionId='16' x:Name="ReconnectingPanel"/>
                <Grid x:ConnectionId='17' x:Name="QRCodeBorder"/><Grid x:ConnectionId='18' x:Name="QRPlaceholder"/><TextBlock x:ConnectionId='19' x:Name="QRStatusText"/>
                <TextBlock x:ConnectionId='20' x:Name="StatusDetail"/><TextBlock x:ConnectionId='21' x:Name="StatusText"/>
                <Image x:ConnectionId='22' x:Name="QRCodeImage"/><Grid x:ConnectionId='23' x:Name="QRProgress"/>
                <Button x:ConnectionId='24' x:Name="ReloadQRButton"/><Button x:ConnectionId='25' x:Name="GenerateQRButton"/>
            </StackPanel>
        </Grid>

        <!-- Main Layout (Visible when logged in) -->
        <SplitView x:ConnectionId='5' x:Name="RootSplitView" 
                   DisplayMode="CompactOverlay" 
                   CompactPaneLength="0"
                   IsPaneOpen="False" 
                   OpenPaneLength="260"
                   PaneBackground="{ThemeResource SystemControlBackgroundAltHighBrush}"
                   Visibility="Collapsed">
            <SplitView.Pane>
                <Grid Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Menu" Margin="20" FontSize="20" FontFamily="{StaticResource MainFontSemibold}"/>
                    
                    <ListView x:ConnectionId='6' x:Name="NavListView" Grid.Row="1" SelectionMode="Single"                                                >
                        <ListViewItem Tag="chats">
                            <StackPanel Orientation="Horizontal" Padding="12">
                                <TextBlock Text="&#xE8BD;" FontFamily="{StaticResource IconFont}" FontSize="18" VerticalAlignment="Center" Width="24"/>
                                <TextBlock Text="Chats" Margin="12,0,0,0" FontSize="14" FontFamily="{StaticResource MainFont}"/>
                            </StackPanel>
                        </ListViewItem>
                        <ListViewItem Tag="debug">
                            <StackPanel Orientation="Horizontal" Padding="12">
                                <TextBlock Text="&#xEBE8;" FontFamily="{StaticResource IconFont}" FontSize="18" VerticalAlignment="Center" Width="24" Foreground="{StaticResource UnisonGreenBrush}"/>
                                <TextBlock Text="Debug &amp; Logs" Margin="12,0,0,0" FontSize="14" FontFamily="{StaticResource MainFont}" Foreground="{StaticResource UnisonGreenBrush}"/>
                            </StackPanel>
                        </ListViewItem>
                    </ListView>
                </Grid>
            </SplitView.Pane>
            
            <SplitView.Content>
                <Grid x:ConnectionId='7' x:Name="RootContentGrid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:ConnectionId='8' x:Name="Column0" Width="320" MaxWidth="400"/>
                        <ColumnDefinition x:ConnectionId='9' x:Name="Column1" Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Left: Chat List -->
                    <views:ChatListView x:ConnectionId='10' x:Name="ChatListPart"                                                                                />
                    
                    <!-- Right: Chat Detail -->
                    <views:ChatDetailView x:ConnectionId='11' x:Name="ChatDetailPart" Grid.Column="1"/>

                    <!-- Full Screen Debug View -->
                    <views:DebugView x:ConnectionId='12' x:Name="DebugPart" Grid.ColumnSpan="2" Visibility="Collapsed"                                        />
                </Grid>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>

